# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GuiddelGuides' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
	pod 'Firebase', :inhibit_warnings => true
  	pod 'Firebase/Auth', :inhibit_warnings => true
  	pod 'Firebase/Storage', :inhibit_warnings => true
	pod 'FirebaseFirestore', :inhibit_warnings => true
  	pod 'FirebaseFirestoreSwift', :inhibit_warnings => true
	pod 'FloatingPanel', :inhibit_warnings => true
	pod 'Alamofire', :inhibit_warnings => true
	pod 'JTAppleCalendar', '~> 7.1.7', :inhibit_warnings => true
	pod 'SwiftLint', :inhibit_warnings => true
	pod 'Firebase/Analytics', :inhibit_warnings => true
	pod 'Firebase/Messaging', :inhibit_warnings => true
	pod 'SideMenu', :inhibit_warnings => true
  # Pods for Guiddel

end
